[package]
name = "solana-hmac-sha256"
authors = ["Dean Little <@deanmlittle>"]
description = "A more efficient, no_std implementation of HMAC-SHA-256 for the Solana SVM"
license = "MIT"
version = "0.3.0"
edition = "2024"
repository = "https://github.com/blueshift-gg/solana-hmac-sha256"
readme = "Readme.md"
keywords = ["solana", "sbpf", "hmac", "no-std", "crypto"]
categories = ["cryptography", "no-std"]
exclude = [".github/", ".gitignore"]

[lib]

[features]
# Transparently forwards to `solana-nostd-sha256/static-syscalls`. Use the
# Upstream BPF / sBPFv3 static-syscall ABI instead of dynamic `extern "C"`
# linking. Requires a toolchain that supports static syscalls.
static-syscalls = ["solana-nostd-sha256/static-syscalls"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }

[dependencies]
solana-nostd-sha256 = "0.2.0"

[dev-dependencies]
svm-unit-test = "0.1.5"
